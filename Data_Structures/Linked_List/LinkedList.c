#include <stdio.h>
#include "SingleList.h"

int main(void){

	printf("Hello World!\n");
	int n = 0;
	NodePtr top, last = NULL;

	if(scanf("%d, %n") != 1) n = 0;
	while(n != 0){
		add	

		if(scanf("%d, %n") != 1) n = 0;
	}

	printList(top);
	return 0;

}
